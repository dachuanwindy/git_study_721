<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans-4.0.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-4.0.xsd">

    <context:property-placeholder ignore-resource-not-found="true" ignore-unresolvable="false"
                                  location="classpath*:properties/ds.properties
                                           ,classpath*:properties/redis.properties,
                                            classpath*:properties/wb.properties"
                                  system-properties-mode="ENVIRONMENT"/>

    <!-- configure the thread pool of scheduler and async, same as @EnableAysnc and @EnableScheduling -->
    <!--     <task:annotation-driven executor="paymentConfigExecutor" scheduler="paymentConfigScheduler"/>
        <task:executor id="paymentConfigExecutor"  pool-size="5-50" queue-capacity="100" rejection-policy="CALLER_RUNS"/>
        <task:scheduler id="paymentConfigScheduler" pool-size="20"/> -->
    <import resource="ds.xml"/>
</beans>