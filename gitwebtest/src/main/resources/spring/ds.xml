<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans-4.0.xsd">


    <!--<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">-->
    <!--<property name="driverClassName" value="${guide.parent.driverClassName}" />-->
    <!--<property name="url" value="${guide.write.jdbcUrl}"></property>-->
    <!--<property name="username" value="${guide.write.user}"></property>-->
    <!--<property name="password" value="${guide.write.pass}"></property>-->
    <!--</bean>-->
    <bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource" destroy-method="close">
        <property name="driverClassName" value="${guide.parent.driverClassName}"/>
        <property name="jdbcUrl" value="${guide.write.jdbcUrl}"/>
        <property name="username" value="${guide.write.user}"/>
        <property name="password" value="${guide.write.pass}"/>
        <!--        <property name="maximumPoolSize" value="${guide.parent.maxActive}"/>-->
        <!--        <property name="minimumIdle" value="${guide.parent.minIdle}"/>-->
    </bean>

    <!-- mybatis factory -->
    <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
        <property name="dataSource" ref="dataSource"/>
        <property name="configLocation" value="classpath:mybatis.xml"/>
        <property name="mapperLocations" value="classpath*:mapper/**/*.xml"/>
    </bean>

    <!-- scan mappers under the dao dir -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <property name="sqlSessionFactoryBeanName" value="sqlSessionFactory"/>
        <property name="basePackage" value="com.bj.tomato.gitwebtest.dao.mapper"/>
    </bean>

</beans>
